# Generation prompts

Store one generation/revision prompt per planned scene here, using the same slug as the scene metadata. Prompts should describe observable actions and layout, not hidden learning logic.
